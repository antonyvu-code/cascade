{
  "name": "cascade",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CASCADE — A Playable Incident. A concept study in single-saturated-accent (one signal colour on near-black): you are the on-call engineer, and your cursor is the last signal alive.",
  "scripts": {
    "dev": "vite --port 5621",
    "build": "vite build",
    "preview": "vite preview --port 5621"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  }
}
